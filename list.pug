---
permalink: "/blog/"
title: hello
---
ul
  each post in collections.post
    li(class=page.url == post.url ? 'active' : '')
      a(href=post.url)
        if post.data.title
          span(class='title')
            = post.data.title
        if post.data.date
          span(class='date')
            = post.data.date
